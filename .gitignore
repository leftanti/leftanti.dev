# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.env
.env.production

# kept locally for Claude Code to read automatically, deliberately not public.
# Sync these across devices via NAS/file-sync if needed — safe for loose files
# like these, never safe for the .git folder itself.
CLAUDE.md
BUILD_PROMPT.md

# editors / OS
# .claude/launch.json is shared dev config and IS committed; the .local
# settings file is a per-machine permission allowlist and is not.
.claude/settings.local.json
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
